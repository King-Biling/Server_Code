# ==============================================================================
# 服务器端运行依赖（Server_Web_UDP）
# ------------------------------------------------------------------------------
# 说明:
#   下列为实际运行所需的第三方库，版本固定为当前验证通过的版本。
#   标准库（socket / threading / json / csv / base64 / gzip 等）无需列出。
#
# 安装:
#   pip install -r requirements.txt
#
# 各依赖用途:
#   flask        Web 控制台与 HTTP API 框架
#   flask-cors   跨域支持（前端页面调用 API）
#   opencv-python  摄像头采集、AprilTag 检测、图像编码/叠加
#   numpy        图像与位姿数值计算（OpenCV 依赖并直接使用）
#   netifaces    自动探测子网广播地址（缺失时回退到默认地址，非强制）
# ==============================================================================
flask==3.1.2
flask-cors==6.0.1
opencv-python==4.12.0.88
numpy==2.2.6
netifaces==0.11.9
