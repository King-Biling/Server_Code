# -*- coding: utf-8 -*-
"""
================================================================================
 模块名称: server_app.__main__
 模块职责: 包级命令行入口，支持 `python -m server_app` 启动
--------------------------------------------------------------------------------
 提供除根目录 web_car_server.py 之外的第二种标准启动方式：

     python -m server_app

 该方式由 Python 以“包”的形式加载 server_app，相对导入天然可用，
 无需任何额外的路径引导。转调 server_app.app.main() 完成全部装配与运行。

 编码格式: UTF-8（无 BOM）
================================================================================
"""

from .app import main

if __name__ == '__main__':
    main()
