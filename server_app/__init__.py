# -*- coding: utf-8 -*-
"""
================================================================================
 模块名称: server_app
 模块职责: 服务器端应用主包（多车协同：编队 / 重构拼接 / 视觉制导 / 遥测广播）
--------------------------------------------------------------------------------
 本包由原单体文件 web_car_server.py 拆分而来，按“功能域”解耦为多个子模块，
 便于多人协同开发、降低单文件合并冲突。各子模块职责概览：

   config          全局常量与可在运行时调整的参数（端口、超时、制导增益等）
   state           跨模块共享的可变状态与线程锁（小车表、重构状态、视觉状态等）
   logging_utils   重构事件的持久化日志与自动归档
   models          纯数据模型（Car / PoseSaveSession）
   net             UDP 服务器、广播服务器及网络辅助函数
   vision          摄像头管理、视觉检测主循环、画面叠加与设备绑定
   guide           末端制导控制器与航向误差计算
   topology        通信拓扑矩阵的构建、下发与缓存
   reconstruct     重构（拼接/分离）状态机与报文处理
   pose_save       小车位姿数据的定时采样与落盘
   routes          Flask 蓝图（按业务域拆分的 HTTP 接口）
   app             Flask 应用装配与程序主入口

 重要约定（保持行为不变的关键）:
   1. 所有“会被重新赋值”的全局标量统一存放于 config / state，
      其它模块必须以“模块名.变量”的形式读写，禁止使用
      `from xxx import 变量` 拷贝可变标量，否则赋值无法跨模块传播。
   2. 网络层与重构层存在相互调用（循环依赖），在函数内部使用
      延迟导入（import inside function）打破环，且不改变原有调用时序。

 编码格式: UTF-8（无 BOM）
================================================================================
"""
